package lcm_msgs;

struct CompressedImage {
  lcm_msgs.msg.Header header;

  string format; // ROS2 string format，例如 "jpeg", "png", "tiff"

  int32_t data_length;    // 数据长度
  byte data[data_length]; // ROS2 uint8[] data（压缩图像缓冲区）
}
